[pytest]
pythonpath = .
testpaths = tests
; 기본 게이트는 외부 의존성 없이 돈다 (CLAUDE.md § Harness Engineering).
; live 테스트는 명시적으로만: pytest -m live
addopts = -q --strict-markers -m "not live"
markers =
    live: 실제 외부 API/DB 를 호출하는 테스트 (기본 게이트에서 제외. 실행: pytest -m live)
